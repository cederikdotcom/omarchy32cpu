#!/bin/bash

# omarchy:summary=Print the detected touchpad or trackpad input identifier

device=$(swaymsg -t get_inputs | jq -r '[.[] | select(.type == "touchpad")] | first.identifier // empty')
[[ -n $device ]] && echo "$device"
