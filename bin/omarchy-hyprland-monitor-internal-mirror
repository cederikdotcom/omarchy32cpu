#!/bin/bash

# omarchy:summary=Mirror the internal display to an external monitor
# omarchy:args=<on|off|toggle|recover>

echo "Display mirroring is not available in Omarchy Omarchy CPU (sway has no output mirroring)" >&2
exit 0
