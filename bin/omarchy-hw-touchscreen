#!/bin/bash

# omarchy:summary=Print the detected touchscreen or tablet input identifier

device=$(swaymsg -t get_inputs | jq -r '[.[] | select(.type == "touch" or .type == "tablet_tool")] | first.identifier // empty')
[[ -n $device ]] && echo "$device"
