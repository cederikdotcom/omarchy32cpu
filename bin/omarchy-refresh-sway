#!/bin/bash

# omarchy:summary=Overwrite the user sway config in ~/.config/sway with the Omarchy default.

omarchy-refresh-config sway/config

mkdir -p ~/.local/state/omarchy/toggles/sway

if [[ -n $SWAYSOCK ]] || pgrep -x sway >/dev/null; then
  swaymsg reload >/dev/null 2>&1 || true
fi
