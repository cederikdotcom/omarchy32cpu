#!/bin/bash

# omarchy:summary=Returns true when sway has an active external monitor
# omarchy:hidden=true

swaymsg -t get_outputs | jq -e '.[] | select(.name | test("^(eDP|LVDS|DSI)-") | not) | select(.active == true)' >/dev/null 2>&1
