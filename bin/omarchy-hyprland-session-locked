#!/bin/bash

# omarchy:summary=Returns true when the session is locked
# omarchy:hidden=true

# swaylock holds sway's ext-session-lock for as long as the session is locked,
# so a live swaylock process is the lock state.
pgrep -x swaylock >/dev/null
